import { CheckRatingDirective } from './check-rating.directive';

describe('CheckRatingDirective', () => {
  it('should create an instance', () => {
    const directive = new CheckRatingDirective();
    expect(directive).toBeTruthy();
  });
});
